<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
</head>
<body style="background-color: #F7FBFC">

    <nav class="navbar navbar-expand-lg navbar-light" style="background-color: #D6E6F2; ">
        <div class="container">
            <a class="navbar-brand" href="/"><i class="bi bi-water"></i></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <!-- Оставил ссылку на главную -->
                    <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/">Главная</a>
                    </li>
                    <!-- остальные заменил на цикл по типам -->
                    {% for type in types %}
                        <li class="nav-item">
                            <!-- добавил href="/?type={{ type.type }}" -->
                            <a class="nav-link" href="/?type={{ type.name }}">{{ type.name|title }}</a>
                        </li>
                    {% endfor %}
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="/search">Поиск</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="/south_pole_objects/create">Добавить</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="/south_pole_objects/type_create">Добавить вид</a>
                    </li>
                </ul>
{# 
                <form action="/set-welcome/">
                    <input type="text" name="massage" class="form-control mr-2 ml-2">
                </form> #}
                <div class="col text-end">
                    <form action="/logout" method="post" >
                        <button type="submit" class="btn btn-logout btn-danger">Выйти</button>
                </form>

                </div>
            </div>
        </div>
    </nav>

    <div style="padding:30px;" class=" d-flex flex-row">
    
    <div style="margin-right:30px;" class="col">
        {% block content %}
            пустота
        {% endblock %}
    </div>

    <div class="col-3">
        <div style="margin-bottom:10px;">
            Последние страницы, которые вы посетили
        </div>
        {% if visitedPages %}
            {% for visitedPage in visitedPages %}
                <div style="background-color: #e6e6fa; padding: 5px 15px; margin-bottom: 5px; border-radius: 10px; word-break: break-all;">
                    <a href="{{ visitedPage|url_decode }}" style="color: black;">{{ visitedPage|url_decode }}</a>
                </div>
            {% endfor %}
        {% else %}
            Нет истории посещений.
        {% endif %}
    </div>
</div>
</body>
</html>
